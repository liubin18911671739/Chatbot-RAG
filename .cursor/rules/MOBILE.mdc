---
description: /MOBILE
globs: mplement React Native UI, navigation (Expo Router), camera/microphone permissions, and communication with Supabase/LLM.
alwaysApply: false
---

## Objective: Implement React Native UI, navigation (Expo Router), camera/microphone permissions, and communication with Supabase/LLM.

## Input: Same as frontend.

## Output: /apps/mobile/*, Expo configuration, native permission documentation.

## Example Usage
```
/MOBILE 用 Expo Router 为 {{FEATURE}} 做两个屏：列表与详情；支持相机/麦克风并上传到 Supabase Storage。
/MOBILE 用 Expo 实现 {{PROJECT}} 的ios和android 应用
```

## Key Points
- expo-router routing
- expo-av/expo-camera
- Secret Management: LLM keys are held 
- exclusively on the server side; clients access your self-hosted API.